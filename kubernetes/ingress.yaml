apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: e-shop
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: e-shop
            port:
              number: 80
